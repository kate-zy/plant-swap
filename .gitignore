# Local secrets — never commit your real admin password.
# Copy config.example.json to config.json for local dev, or just use env vars.
config.json

# Runtime data — generated by the app, not part of the source.
data/*.json
data/uploads/

# Node
node_modules/
npm-debug.log*
_to_delete/
